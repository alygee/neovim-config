-- require('colorbuddy').colorscheme('cobalt2')
require('material').setup({
    -- ... other settings
    disable = {
        -- ... other settings
        background = true,
    },
})

vim.cmd 'colorscheme material'
